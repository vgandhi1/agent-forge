# Mirrors pyproject.toml dependencies. Prefer: `uv sync` (uses uv.lock) for a reproducible env.
anthropic>=0.40.0
aiosqlite>=0.20.0
pydantic>=2.0.0
rich>=13.7.0
python-dotenv>=1.0.0
textual>=0.79.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
